/**
 * 1-swap.c - Swap the values of two integers
 * @n: swap the values
 */
void swap_int(int *a, int *b)
{
	 int a, b;
    printf("a=98 ");
    scanf("%d", &a);
    printf("b=42 ");
    scanf("%d", &b);
    swap(a, b);
    getch();
}
